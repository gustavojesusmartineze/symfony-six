{% extends 'base.html.twig' %}

{% block body %}
{# dump() #}
{# dump(search) #}
<h1>Welcome, home page</h1>

<div>
  {% for comment in comments %}
    <p>
      {{ comment.body }}
    </p>
  {% endfor %}
</div>
{% endblock %}